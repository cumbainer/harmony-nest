import {Form} from "react-router-dom";

const Login = () => {
    return (
        <Form method="post">
            <button className="text-5xl text-center text-white">Login with spotify</button>
        </Form>
    );
};
export default Login;